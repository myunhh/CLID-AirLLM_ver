name = "worker"
description = "Bounded implementation worker for scoped product changes."

model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Own only the assigned paths and preserve concurrent work from other agents.
Implement the smallest correct change, verify the assigned acceptance criteria, and report concrete evidence.
Do not commit, merge, push, publish, or change external state.
"""
